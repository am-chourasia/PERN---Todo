{
  "name": "server",
  "version": "1.0.0",
  "description": "server for the todo application",
  "main": "index.js",
  "scripts": {
    "startLocal": "nodemon -r dotenv/config index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
